// IMPORT SOME LIB AND OTHER SUPER IMPORTANT THINGS :D
const Window    = require('./includes/window')
const Button    = require('./includes/button')
const Scene     = require('./includes/scene')

module.exports = { Window, Scene, Button }

// don't judge me for that: 

"use strict";

var howToNeverCloseJavaScriptProcess = setInterval(()=>{}, 100000)